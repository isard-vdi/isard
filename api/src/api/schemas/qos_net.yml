id:
  required: true
  empty: false
  default_setter: genuuid
  coerce: sanitize
allowed:
  type: dict
  schema: allowed
description:
  type: string
  coerce: sanitize
name:
  type: string
  required: true
  empty: false
  coerce: sanitize
bandwith:
  type: dict
  schema:
    inbound:
      type: dict
    outbound:
      type: dict
