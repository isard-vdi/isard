id:
  required: true
  type: string
  default_setter: genuuid
  coerce: sanitize
name:
  maxlength: 50
  required: true
  type: string
  coerce: sanitize
memory:
  required: true
  type: string
  coerce: sanitize
model:
  type: string
  coerce: sanitize
description:
  maxlength: 255
  type: string
  coerce: sanitize
brand:
  type: string
  coerce: sanitize
reservable_type:
  allowed:
    - gpus
    - usbs