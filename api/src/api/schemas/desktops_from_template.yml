name:
  maxlength: 50
  required: true
  type: string
  minlength: 4
  coerce: sanitize
description:
  maxlength: 255
  type: string
  required: false
  coerce: sanitize
template_id:
  type: string
  required: true
  coerce: sanitize
allowed:
  required: true
  schema: allowed