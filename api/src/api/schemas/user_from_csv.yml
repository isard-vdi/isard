username:
  required: true
  empty: false
  regex: "^[A-Za-z0-9._@%+-]+$"
  maxlength: 40
  coerce: sanitize
name:
  maxlength: 50
  required: true
  empty: false
  coerce: sanitize
email:
  type: string
  coerce: sanitize
group:
  required: true
  empty: false
  coerce: sanitize
category:
  required: true
  empty: false
  coerce: sanitize
role:
  required: true
  empty: false
  coerce: sanitize

