spec:
  inputs:
    use-image-build-cache:
      type: boolean
      default: true
    allow-protobuf-lint-failure:
      type: boolean
      default: false
---
# https://docs.gitlab.com/ee/ci/yaml/index.html#switch-between-branch-pipelines-and-merge-request-pipelines
workflow:
  # `auto_cancel_pending_pipelines` only kills a superseded pipeline's *pending*
  # jobs; the running ones keep burning the project's active-job budget until
  # they finish. This also cancels those, so the newer pipeline gets the slots.
  auto_cancel:
    on_new_commit: interruptible
  rules:
    - if: $CI_COMMIT_BRANCH && $CI_PIPELINE_SOURCE == "push" && $CI_OPEN_MERGE_REQUESTS || $CI_COMMIT_TAG
      when: never
    - when: always

# https://docs.gitlab.com/ee/ci/docker/using_docker_build.html#use-docker-in-docker
variables:
  # When you use the dind service, you must instruct Docker to talk with
  # the daemon started inside of the service. The daemon is available
  # with a network connection instead of the default
  # /var/run/docker.sock socket. Docker 19.03 does this automatically
  # by setting the DOCKER_HOST in
  # https://github.com/docker-library/docker/blob/d45051476babc297257df490d22cbd806f1b11e4/19.03/docker-entrypoint.sh#L23-L29
  #
  # The 'docker' hostname is the alias of the service container as described at
  # https://docs.gitlab.com/ee/ci/services/#accessing-the-services.
  #
  # Specify to Docker where to create the certificates. Docker
  # creates them automatically on boot, and creates
  # `/certs/client` to share between the service and job
  # container, thanks to volume mount from config.toml
  DOCKER_HOST: tcp://docker:2376
  DOCKER_VERSION: "28"
  DOCKER_TLS_CERTDIR: "/certs"
  GITLAB_ADVANCED_SAST_ENABLED: "true"
  MAX_UNVERIFIED_CORES: "16"
  # Cache bun's content-addressed global install store (hash-verified, self-healing)
  # instead of the derived node_modules tree. A corrupt or truncated file inside a
  # cached node_modules (e.g. a 0-byte dependency file) otherwise persists across
  # pipelines forever, because the cache key is the lockfile hash and
  # `bun install --frozen-lockfile` reuses the restored tree without per-file
  # integrity checks. Caching the global store lets bun detect a bad entry by hash
  # and refetch it, rebuilding node_modules fresh each run.
  BUN_INSTALL_CACHE_DIR: "$CI_PROJECT_DIR/.bun-cache"
  # Secret Detection scans third-party Go module testdata fixtures (RSA/SSH keys,
  # AWS sample IDs, etc.) inside .go-mod-cache as critical findings. Those are
  # not project secrets — they ship inside dependency tarballs. Excluding the
  # dependency caches keeps the report focused on this repo's own code.
  SECRET_DETECTION_EXCLUDED_PATHS: ".go-mod-cache,.go-build-cache,vendor,node_modules,old-frontend/node_modules,component/frontend/node_modules,testing/e2e/node_modules"
  # GitLab only stores cache paths that live INSIDE the project directory: with
  # Go's default GOMODCACHE (/go/pkg/mod) the runner logs `processPath: artifact
  # path is not a subpath of project directory` and silently skips it, so the
  # module cache was never actually stored. Point both Go caches into the tree.
  GOMODCACHE: "$CI_PROJECT_DIR/.go-mod-cache"
  GOCACHE: "$CI_PROJECT_DIR/.go-build-cache"
  # Same reason, and it was only set on the python test template: the lint jobs
  # and integration-real cached `.uv-cache` while uv wrote to its default
  # location inside the container, so they re-resolved every dependency —
  # including the two fetched straight from a git forge — on every run.
  UV_CACHE_DIR: "$CI_PROJECT_DIR/.uv-cache"

default:
  # Every job only reads the tree or pushes SHA-tagged images, so cancelling one
  # mid-flight leaves nothing half-applied. Required by `auto_cancel` above.
  interruptible: true
  retry: 1

# A cache declared under `default:` is downloaded AND re-uploaded by EVERY job in
# the pipeline. With an S3 cache backend each entry costs a round trip both ways,
# and the four entries together are ~216k files. Most jobs read none of them: the
# `docker-image` builds only drive docker-compose and the `docker-tag` jobs run a
# single `crane tag`. Measured on a saturated runner, `restore_cache` was 1260 s
# of a 1412 s `docker-tag` job whose real work is 1.3 s. Each job now declares
# only what it reads.
#
# The key files must be the lockfiles that actually exist: a `key: files:` entry
# naming a missing file silently degrades to a constant, so the cache stops
# tracking the dependencies it is meant to key on.
# Each anchor is a single cache ENTRY, not a list, so a job that needs two of
# them can compose both under one `cache:` key.
.cache-go: &cache_go
  key:
    files:
      - go.mod
      - go.sum
  paths:
    - .go-mod-cache
    - .go-build-cache

.cache-bun: &cache_bun
  key:
    files:
      - component/frontend/bun.lock
      - old-frontend/bun.lock
  paths:
    - .bun-cache

.cache-e2e: &cache_e2e
  key:
    files:
      - testing/e2e/package.json
      - testing/e2e/bun.lock
  paths:
    - testing/e2e/node_modules

.cache-uv: &cache_uv
  key:
    files:
      - uv.lock
  paths:
    - .uv-cache

include:
  - template: Jobs/Dependency-Scanning.gitlab-ci.yml
  - template: Jobs/Container-Scanning.gitlab-ci.yml
  - template: Jobs/SAST.gitlab-ci.yml
  - template: Jobs/Secret-Detection.gitlab-ci.yml

stages:
  - lint
  - build-codegen
  - generate
  - unit-test
  - build
  - build-no-cache
  - test
  - release
  - container-scan

commit-lint:
  stage: lint
  cache:
    - *cache_bun
  image: oven/bun:1.3.13-alpine@sha256:4de475389889577f346c636f956b42a5c31501b654664e9ae5726f94d7bb5349
  before_script:
    - apk add --no-cache git
    - bun install --frozen-lockfile
  script:
    - git fetch https://gitlab.com/isard/isardvdi.git "${CI_MERGE_REQUEST_TARGET_BRANCH_NAME:-main}"
    - bunx commitlint --from=$(git merge-base HEAD FETCH_HEAD)

version:
  stage: lint
  cache:
    - *cache_bun
  image: oven/bun:1.3.13-alpine@sha256:4de475389889577f346c636f956b42a5c31501b654664e9ae5726f94d7bb5349
  rules:
    # semantic-release checks repository write access and we cannot use
    # GITLAB_TOKEN in unprotected branches due to security reasons.
    # https://github.com/semantic-release/semantic-release/issues/2207
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  before_script:
    - apk add --no-cache git
    - bun install --frozen-lockfile
  script:
    - bunx semantic-release --dry-run --branches $CI_COMMIT_REF_NAME
  artifacts:
    paths:
      - .VERSION

check-alloy-config:
  stage: lint
  image: alpine:3.23@sha256:fd791d74b68913cbb027c6546007b3f0d3bc45125f797758156952bc2d6daf40
  before_script:
    - apk add --no-cache alloy make
  script:
    - make lint-alloy

# The uv cache is keyed on uv.lock, so whenever that changes — or whenever the
# cache configuration itself changes, which alters the key's index prefix — every
# python job starts with an empty cache and they all resolve the dependency set at
# the same moment. Two of those dependencies are source tarballs fetched straight
# from a git forge, and twenty simultaneous fetches of the same tarball is what
# that forge refuses, with `http2: refused stream`. The jobs then retry, in
# parallel, and hit the same wall.
#
# This job populates the cache once, and every consumer below only reads it. On a
# cold key one job fetches instead of twenty; on a warm key it resolves from the
# cache in seconds and costs nothing. Verified offline (`--network none`,
# `--offline`): with this cache present, `uv sync` for the consumer packages and
# for the dev group completes without touching the network at all.
#
# It syncs the packages the uv-image jobs actually name rather than
# `--all-packages`: the latter pulls in isardvdi-engine, whose libvirt-python
# needs headers this image does not carry, and whose suite runs in a different
# image anyway.
warm-uv-cache:
  stage: lint
  needs: []
  # Only this job writes the cache. If it fails the consumers still run, they just
  # resolve from the network as they did before.
  allow_failure: true
  retry: 2
  cache:
    - <<: *cache_uv
      policy: pull-push
  image: ghcr.io/astral-sh/uv:0.11.23-python3.13-alpine@sha256:4116326f5dc6815abc4d0a5a3846b735488ceb00cd3dc16da2a78577d30d6e0b
  before_script:
    - apk add --no-cache git make
  script:
    - uv sync --frozen --no-dev --group test
      --package isard-anonymize-db --package isardvdi-apiv4
      --package isardvdi-changefeed --package isardvdi-change-handler
      --package isardvdi-codegen --package isardvdi-common
      --package isardvdi-notifier --package isardvdi-openapi
      --package isardvdi-scheduler --package isardvdi-socketio
      --package isardvdi-storage --package isardvdi-vpn
      --package isardvdi-webapp
    - uv sync --frozen --only-group dev

.lint-uv-job:
  stage: lint
  needs: ["warm-uv-cache"]
  cache:
    - <<: *cache_uv
      policy: pull
  image: ghcr.io/astral-sh/uv:0.11.23-python3.13-alpine@sha256:4116326f5dc6815abc4d0a5a3846b735488ceb00cd3dc16da2a78577d30d6e0b
  before_script:
    - apk add --no-cache make
    - uv sync --only-group dev

check-python:
  extends: .lint-uv-job
  script:
    - make lint-python

check-system-deps:
  extends: .lint-uv-job
  script:
    - make lint-system-deps

check-golang:
  stage: lint
  cache:
    - *cache_go
  image: golangci/golangci-lint@sha256:5cceeef04e53efe1470638d4b4b4f5ceefd574955ab3941b2d9a68a8c9ad5240
  script:
    - make lint-go

check-frontend:
  stage: lint
  cache:
    - *cache_bun
  image: oven/bun:1.3.13-alpine@sha256:4de475389889577f346c636f956b42a5c31501b654664e9ae5726f94d7bb5349
  parallel:
    matrix:
      - JOB: "format"
      - JOB: "lint"
      # - JOB: "type-check"
      #   # TODO: Remove this when all type errors are fixed
      #   ALLOW_FAILURE: "true"
  rules:
    - if: $ALLOW_FAILURE == "true"
      allow_failure: true
    - if: $ALLOW_FAILURE != "true"
      allow_failure: false
  before_script:
    - apk add --no-cache make
  script:
    - make lint-frontend-$JOB

check-old-frontend:
  stage: lint
  cache:
    - *cache_bun
  image: oven/bun:1.3.13-alpine@sha256:4de475389889577f346c636f956b42a5c31501b654664e9ae5726f94d7bb5349
  before_script:
    - apk add --no-cache make
  script:
    - make lint-old-frontend

check-protobuf:
  stage: lint
  image:
    name: bufbuild/buf:1.44.0@sha256:7e06f3027a16f8f63c25db6a1b5544866b677ad16e3907c14563c4a61d408228
    entrypoint: [""]
  allow_failure: $[[ inputs.allow-protobuf-lint-failure ]]
  # bufbuild/buf image is distroless — no package manager to install make.
  # Keep invocation inline; commands match `make lint-protobuf`.
  script:
    - buf lint
    - buf breaking --against https://gitlab.com/isard/isardvdi.git

docker-image-codegen:
  image: "docker:${DOCKER_VERSION}@sha256:2a232a42256f70d78e3cc5d2b5d6b3276710a0de0596c145f627ecfae90282ac"
  stage: build-codegen
  services:
    - name: "docker:${DOCKER_VERSION}-dind-rootless@sha256:7c3e797187e43738220462658f4586572cbd3bf009f728b21e34d9c5c06ce431"
  parallel:
    matrix:
      - DOCKERFILE: docker/codegen/Dockerfile
        IMAGE: codegen
  before_script:
    - timeout 300 sh -c 'until docker info >/dev/null 2>&1; do sleep 2; done'
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
  script:
    - |
      if $[[ inputs.use-image-build-cache ]]
      then
        CACHE_ARGS="--cache-from registry.gitlab.com/isard/isardvdi/$IMAGE:main --cache-to type=inline"
        if [ -n "$BUILDKIT_CACHE_REGISTRY" ]; then
          CACHE_ARGS="--cache-from $BUILDKIT_CACHE_REGISTRY/isard/isardvdi/$IMAGE:latest $CACHE_ARGS"
        fi
        docker build -f $DOCKERFILE $CACHE_ARGS -t $CI_REGISTRY_IMAGE/$IMAGE:$CI_COMMIT_REF_SLUG .
      else
        docker build -f $DOCKERFILE -t $CI_REGISTRY_IMAGE/$IMAGE:$CI_COMMIT_REF_SLUG .
      fi
    - docker tag $CI_REGISTRY_IMAGE/$IMAGE:$CI_COMMIT_REF_SLUG $CI_REGISTRY_IMAGE/$IMAGE:git-$CI_COMMIT_SHORT_SHA
    - docker push $CI_REGISTRY_IMAGE/$IMAGE:git-$CI_COMMIT_SHORT_SHA
    - docker push $CI_REGISTRY_IMAGE/$IMAGE:$CI_COMMIT_REF_SLUG
    - docker tag $CI_REGISTRY_IMAGE/$IMAGE:$CI_COMMIT_REF_SLUG $BUILDKIT_CACHE_REGISTRY/isard/isardvdi/$IMAGE:latest || true
    - docker push $BUILDKIT_CACHE_REGISTRY/isard/isardvdi/$IMAGE:latest || true

gen-protobuf:
  stage: generate
  image: "docker:${DOCKER_VERSION}@sha256:2a232a42256f70d78e3cc5d2b5d6b3276710a0de0596c145f627ecfae90282ac"
  services:
    - name: "docker:${DOCKER_VERSION}-dind-rootless@sha256:7c3e797187e43738220462658f4586572cbd3bf009f728b21e34d9c5c06ce431"
  before_script:
    - timeout 300 sh -c 'until docker info >/dev/null 2>&1; do sleep 2; done'
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
  script:
    - docker run -u $(id -u) -v "$(pwd):/build" "$CI_REGISTRY_IMAGE/codegen:git-$CI_COMMIT_SHORT_SHA"
  artifacts:
    paths:
      - pkg/gen
      - component/frontend/src/gen
      - testing/e2e/src/gen
      - "**/testing_*_mock.go"
      # openapi-python-client output. Each client's src/<pkg>/__init__.py is
      # tracked but the rest of the tree (client.py, errors.py, types.py,
      # api/, models/) is gitignored — codegen regenerates it. Without
      # propagating these paths the downstream unit-test-apiv4 /
      # unit-test-webapp jobs see only __init__.py whose first line is
      # `from .client import …`, which raises ModuleNotFoundError at import.
      - component/_common/isardvdi_apiv4_client/src/isardvdi_apiv4_client
      - component/_common/isardvdi_authentication_client/src/isardvdi_authentication_client
      - component/_common/isardvdi_notifier_client/src/isardvdi_notifier_client
      - component/_common/isardvdi_scheduler_client/src/isardvdi_scheduler_client

docker-compose-build:
  stage: generate
  needs: []
  image: "docker:${DOCKER_VERSION}@sha256:2a232a42256f70d78e3cc5d2b5d6b3276710a0de0596c145f627ecfae90282ac"
  services:
    - name: "docker:${DOCKER_VERSION}-dind-rootless@sha256:7c3e797187e43738220462658f4586572cbd3bf009f728b21e34d9c5c06ce431"
  before_script:
    - apk add bash git docker-compose yq
  script:
    - cp isardvdi.cfg.example isardvdi.build.cfg
    - echo "DOCKER_IMAGE_PREFIX=${CI_REGISTRY_IMAGE}/" >> isardvdi.build.cfg
    - echo "DOCKER_IMAGE_TAG=$CI_COMMIT_REF_SLUG" >> isardvdi.build.cfg
    - echo "USAGE=build" >> isardvdi.build.cfg
    - echo "BASTION_ENABLED=true" >> isardvdi.build.cfg
    - echo "ENABLE_OPENAPI=true" >> isardvdi.build.cfg
    - cp isardvdi.build.cfg isardvdi.infrastructure.build.cfg
    - echo "INFRASTRUCTURE_MANAGEMENT=true" >> isardvdi.infrastructure.build.cfg
    - cp isardvdi.build.cfg isardvdi.video-standalone.build.cfg
    - echo "FLAVOUR=video-standalone" >> isardvdi.video-standalone.build.cfg
    - cp isardvdi.build.cfg isardvdi.monitor.build.cfg
    - echo "FLAVOUR=monitor" >> isardvdi.monitor.build.cfg
    - cp isardvdi.build.cfg isardvdi.check.build.cfg
    - echo "FLAVOUR=check" >> isardvdi.check.build.cfg
    - BACKUP_DB_ENABLED=true CODEGEN=false ./build.sh
    - |-
      # Cache from the branch we are merging into (apiv4-integration today, main
      # after the apiv4->main cutover — self-adjusting), with :main as fallback.
      CACHE_REF="${CI_MERGE_REQUEST_TARGET_BRANCH_NAME:-${CI_COMMIT_REF_SLUG:-main}}"
      # This job runs on ONE runner but the compose files it publishes are an
      # artifact consumed by every `docker-image` job, on EVERY runner. Writing
      # this runner's own BUILDKIT_CACHE_REGISTRY into cache_from therefore sent
      # the other runner's builds to a registry it has no insecure entry for,
      # which fails as `http: server gave HTTP response to HTTPS client` and
      # drops them to pulling cache manifests over the internet. Emit a
      # placeholder instead; each build resolves it to its OWN registry, which is
      # also the one it pushes to at the end of the job.
      for file in docker-compose*.yml; do
        cp "$file" "no-cache-$file"
        yq -i '.services |= map_values(.build.cache_from = [ "LOCAL_BUILDKIT_CACHE/isard/isardvdi/" + (.container_name | sub("isard-", "")) + ":latest", "registry.gitlab.com/isard/isardvdi/" + (.container_name | sub("isard-", "")) + ":'"$CACHE_REF"'", "registry.gitlab.com/isard/isardvdi/" + (.container_name | sub("isard-", "")) + ":main" ]) |= map_values(.build.cache_to = [ "type=inline" ])' $file
      done
  artifacts:
    paths:
      - docker-compose*.yml
      - no-cache-docker-compose*.yml

unit-test-go:
  stage: unit-test
  needs: ["gen-protobuf"]
  image: golang:1.25.8@sha256:3ac2864710f25e84381bf5d4272261c7ba73ada0339d62034df4de20dabb33ca
  cache:
    - *cache_go
  before_script:
    - apt-get update && apt-get install -y libvirt-dev make
  script:
    - make ci-test-go
  artifacts:
    when: always
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage.xml
      junit: report.xml
  coverage: /total:\s+\(statements\)\s+\d+.\d+%/

# Behavioural unit tests for the storage-governor alert rules, run under the SAME
# engine the stack runs (vmalert / MetricsQL) — promtool green is not vmalert
# green, and the Go test above only checks that the rules NAME real metrics, not
# what they DO. Pinned to the vmalert-tool matching the runtime vmalert in
# docker-compose-parts/monitor.yml; the guard fails CI if the two ever drift, so
# this pin cannot silently diverge from what the stack actually runs.
unit-test-vmalert:
  stage: unit-test
  needs: []
  image:
    name: victoriametrics/vmalert-tool:v1.147.0
    entrypoint: [""]
  script:
    - runtime=$(grep -oE 'victoriametrics/vmalert:v[0-9.]+' docker-compose-parts/monitor.yml | head -1 | sed 's/.*://')
    - tool=$(/vmalert-tool-prod --version 2>&1 | grep -oE 'v[0-9]+\.[0-9]+\.[0-9]+' | head -1)
    - '[ -n "$runtime" ] || { echo "FAIL: cannot read the runtime vmalert pin from docker-compose-parts/monitor.yml"; exit 1; }'
    - '[ "$runtime" = "$tool" ] || { echo "FAIL: vmalert-tool $tool != runtime vmalert $runtime (docker-compose-parts/monitor.yml) — bump this job image and monitor.yml together"; exit 1; }'
    - /vmalert-tool-prod unittest --files docker/vmalert/rules/storage_governor.test.yml

# Shared Python test config — uv cache dir cached via default cache entry.
.python-test-base:
  stage: unit-test
  cache:
    - <<: *cache_uv
      policy: pull
  needs: ["gen-protobuf", "warm-uv-cache"]
  image: ghcr.io/astral-sh/uv:0.11.23-python3.13-alpine@sha256:4116326f5dc6815abc4d0a5a3846b735488ceb00cd3dc16da2a78577d30d6e0b
  before_script:
    - apk add --no-cache git make

.python-test-job:
  extends: .python-test-base
  artifacts:
    when: always
    reports:
      coverage_report:
        coverage_format: cobertura
        path: ${COVERAGE_DIR}/coverage.xml
      junit: ${COVERAGE_DIR}/report.xml
  coverage: '/TOTAL\s+\d+\s+\d+\s+\d+%/'

# Python tests that need a REAL Redis, declared once for every job that does.
#
# THE CRITERION, in one place: a suite qualifies when what it asserts on is
# Redis's own behaviour rather than our code's use of it — an rq job graph
# (hashes, dependency links, registries, Job.cancel), or a Lua script whose
# effect a mocked connection cannot report. For those, a fake Redis is a second
# implementation of the very thing under test, so a divergence between it and
# the real server surfaces as confidence instead of as a failure.
#
# This is never a service added to make a test pass. Those tests pass; without
# a server they SKIP, which is worse — the suite goes green having asserted
# nothing. Adding it is what makes them run at all.
#
# Jobs opt in by extending this. Anything extending .python-test-job keeps
# exactly the environment it has today.
#
# Pinned to the same image and digest as the stack's own isard-redis, so CI
# exercises the server version production runs. ISARD_TEST_REDIS carries the
# same server to the suites that take a URL, so a job never has to invent its
# own way of saying "there is a Redis here".
.python-test-job-redis:
  extends: .python-test-job
  services:
    - name: redis:7.4-alpine3.21@sha256:6ab0b6e7381779332f97b8ca76193e45b0756f38d4c0dcda72dbb3c32061ab99
      alias: redis
  variables:
    REDIS_HOST: redis
    ISARD_TEST_REDIS: "redis://redis:6379/9"

unit-test-apiv4:
  extends: .python-test-job
  # A real redis, because the retry guard that refuses a task whose direct
  # dependent is CANCELED rests on rq's own admission rule, and that premise is
  # proved by running rq rather than by asserting it. The service is declared
  # here and NOT on ``.python-test-base``: sixteen other python jobs extend that
  # base and none of them wants a redis container. Same image and digest the
  # stack runs, so the proof exercises the redis we ship.
  services:
    - name: redis:7.4-alpine3.21@sha256:6ab0b6e7381779332f97b8ca76193e45b0756f38d4c0dcda72dbb3c32061ab99
      alias: redis
  variables:
    COVERAGE_DIR: component/apiv4/src
    USAGE: "production"
    REDIS_HOST: redis
  script:
    - make ci-test-apiv4

unit-test-change-handler:
  # Real rq graphs: which members of a chain can be reached from its root.
  extends: .python-test-job-redis
  variables:
    COVERAGE_DIR: component/change-handler/src
  script:
    - make ci-test-change-handler

unit-test-changefeed:
  extends: .python-test-job
  variables:
    COVERAGE_DIR: component/changefeed/src
  script:
    - make ci-test-changefeed

unit-test-webapp:
  extends: .python-test-job
  variables:
    COVERAGE_DIR: webapp/webapp
    USAGE: "test"
    API_ISARDVDI_SECRET: "ci-test-secret"
  script:
    - make ci-test-webapp

unit-test-common:
  # The ended_at stamp is a Lua script: a mocked connection cannot say whether
  # it wrote, and the empty-field trap it guards was found by running it for
  # real. Those tests have been skipping since they were written.
  extends: .python-test-job-redis
  variables:
    COVERAGE_DIR: component/_common/src
  script:
    - make ci-test-common

unit-test-hypervisor-lib:
  # Hypervisor GPU lib suites. Dependency-free
  # pytest: the shared gpu modules load from
  # component/_common/src/isardvdi_common/lib via gpu_probe._load_shared.
  extends: .python-test-base
  stage: unit-test
  script:
    - make ci-test-hypervisor-lib
  artifacts:
    when: always
    reports:
      junit: docker/hypervisor/src/lib/report.xml

unit-test-hypervisor-ovs:
  # OVS worker flow-generation suite for the per-interface lab_opts feature
  # Pure-stdlib pytest: ovs-worker.py is loaded via importlib and
  # subprocess.run is monkeypatched, so no OVS bridge, root, or extra runtime
  # deps are needed. The engine domain_xml lab_opts test rides unit-test-engine
  # and the apiv4 validation test rides unit-test-apiv4; this closes the last
  # gap (docker/hypervisor/src/ovs is not covered by unit-test-hypervisor-lib).
  extends: .python-test-base
  stage: unit-test
  script:
    - make ci-test-hypervisor-ovs
  artifacts:
    when: always
    reports:
      junit: docker/hypervisor/src/ovs/report.xml

check-codegen-freshness:
  # Catches commits that modify ``pkg/oas/apiv4/*.json`` / ``.proto`` /
  # SDK interfaces without regenerating the tracked mocks. ``pkg/gen/**``
  # and ``component/frontend/src/gen/**`` are gitignored so they do not
  # participate, but ``pkg/sdk/testing_*_mock.go`` IS tracked and must
  # be kept current. The ``gen-protobuf`` job produces fresh artifacts;
  # we compare them to what was committed.
  stage: unit-test
  needs: ["gen-protobuf"]
  image: alpine:3.22@sha256:14358309a308569c32bdc37e2e0e9694be33a9d99e68afb0f5ff33cc1f695dce
  before_script:
    - apk add --no-cache git
    - git config --global --add safe.directory "$CI_PROJECT_DIR"
  script:
    - |-
      if ! git diff --exit-code -- '**/testing_*_mock.go' pkg/oas; then
        echo ""
        echo "ERROR: Generated code is out of date."
        echo "Re-run codegen locally (bash build.sh with CODEGEN unset)"
        echo "and commit the regenerated mocks + oas."
        exit 1
      fi
    - echo "Generated code is up to date."

unit-test-socketio:
  extends: .python-test-job
  variables:
    COVERAGE_DIR: component/socketio/src
  script:
    - make ci-test-socketio

unit-test-openapi:
  extends: .python-test-job
  variables:
    COVERAGE_DIR: component/openapi/src
  script:
    - make ci-test-openapi

unit-test-notifier:
  extends: .python-test-job
  variables:
    COVERAGE_DIR: notifier
  script:
    - make ci-test-notifier

unit-test-scheduler:
  extends: .python-test-job
  variables:
    COVERAGE_DIR: scheduler
  script:
    - make ci-test-scheduler

unit-test-apiv4-client:
  # isardvdi_apiv4_client / isardvdi_apiv4_client_auth suites. The generated
  # isardvdi_apiv4_client/src tree is gitignored and produced by gen-protobuf,
  # so this job depends on it (like unit-test-apiv4 / unit-test-webapp).
  extends: .python-test-base
  stage: unit-test
  script:
    - make ci-test-apiv4-client
  artifacts:
    when: always
    reports:
      junit: component/_common/isardvdi_apiv4_client/report.xml

unit-test-vpn:
  # isard-vpn wgtools/wgadmin/simple_iptools/db suites. test_wgadmin_dispatch
  # imports the generated changefeed_subscribers package (pkg/gen), so this
  # job depends on gen-protobuf via .python-test-base.
  extends: .python-test-base
  stage: unit-test
  script:
    - make ci-test-vpn
  artifacts:
    when: always
    reports:
      junit: docker/vpn/report.xml

unit-test-codegen:
  # Changefeed AsyncAPI / subscriber generator suites. One test compiles the
  # real generated changefeed_models tree (pkg/gen) when present, otherwise
  # skips; gen-protobuf (via .python-test-base) makes it present.
  extends: .python-test-base
  stage: unit-test
  script:
    - make ci-test-codegen
  artifacts:
    when: always
    reports:
      junit: docker/codegen/report.xml

unit-test-anonymize-db:
  # sysadm/anonymize-db scrub/prune/safety/streaming/fake-storage suites.
  # Self-contained (no generated code) but kept in the shared unit-test stage.
  extends: .python-test-base
  stage: unit-test
  needs: []
  script:
    - make ci-test-anonymize-db
  artifacts:
    when: always
    reports:
      junit: sysadm/anonymize-db/report.xml

unit-test-frontend:
  # Vue 3 (component/frontend) vitest unit suites. The specs vi.mock the
  # generated @/gen/oas/apiv4 client, but the alias must still resolve, so the
  # gen-protobuf-produced component/frontend/src/gen tree must be present.
  stage: unit-test
  cache:
    - *cache_bun
  needs: ["gen-protobuf"]
  image: oven/bun:1.3.13-alpine@sha256:4de475389889577f346c636f956b42a5c31501b654664e9ae5726f94d7bb5349
  before_script:
    - apk add --no-cache make
  script:
    - make ci-test-frontend
  artifacts:
    when: always
    reports:
      junit: component/frontend/report.xml

unit-test-sparsify:
  # Recovery-trap suite for the sparsify utility (docker/storage/utils/tests).
  # Plain bash, no python and no generated code, but it drives real qcow2 files
  # and a live lock holder, so it needs qemu-img and qemu-io.
  stage: unit-test
  needs: []
  image: alpine:3.22@sha256:14358309a308569c32bdc37e2e0e9694be33a9d99e68afb0f5ff33cc1f695dce
  before_script:
    - apk add --no-cache bash make qemu-img
  script:
    - make test-sparsify

unit-test-storage-utils:
  # Sparsify-backup classifier in the `storage` cleanup CLI
  # (docker/storage/utils/tests). Pure-stdlib pytest: the CLI is loaded via
  # importlib (it has no .py suffix) and the qemu-img calls its helpers share
  # are replaced, so no qemu binaries, no lock holder and no root are needed.
  extends: .python-test-base
  stage: unit-test
  script:
    - make ci-test-storage-utils
  artifacts:
    when: always
    reports:
      junit: docker/storage/utils/report.xml

unit-test-storage:
  # isard-storage worker failure-path suite (docker/storage/task/tests): the
  # raise-on-failure contract, convert partial-dest cleanup, and the delete
  # reachable-mount guard. The storage package depends on the generated
  # isardvdi_apiv4_client, so this job depends on gen-protobuf via
  # .python-test-base.
  extends: .python-test-base
  stage: unit-test
  script:
    - make ci-test-storage
  artifacts:
    when: always
    reports:
      junit: docker/storage/report.xml

docker-image:
  image: "docker:${DOCKER_VERSION}@sha256:2a232a42256f70d78e3cc5d2b5d6b3276710a0de0596c145f627ecfae90282ac"
  stage: build
  needs:
    - docker-compose-build
    - gen-protobuf
  services:
    - name: "docker:${DOCKER_VERSION}-dind-rootless@sha256:7c3e797187e43738220462658f4586572cbd3bf009f728b21e34d9c5c06ce431"
  parallel:
    matrix:
      - DOCKER_COMPOSE: build
        IMAGE: &containers
          - apiv4
          - changefeed
          - change-handler
          - scheduler
          - authentication
          - backupninja
          - bastion
          - db
          - engine
          - grafana
          - guac
          - hypervisor
          - notifier
          - openapi
          - sessions
          - socketio
          - squid
          - static
          - stats-go
          - stats-rethinkdb
          - storage
          - webapp
          - websockify
      - DOCKER_COMPOSE: build
        IMAGE: vpn
      - DOCKER_COMPOSE: build
        IMAGE: portal
      - DOCKER_COMPOSE: infrastructure.build
        IMAGE: orchestrator
      - DOCKER_COMPOSE: video-standalone.build
        IMAGE: video
      - DOCKER_COMPOSE: monitor.build
        IMAGE: monitor-proxy
      - DOCKER_COMPOSE: check.build
        IMAGE:
          - check
          - check-client
  before_script:
    - apk add git docker-compose
    - timeout 300 sh -c 'until docker info >/dev/null 2>&1; do sleep 2; done'
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
  script:
    - git submodule update --init --recursive
    - |
      if $[[ inputs.use-image-build-cache ]]
      then
        DOCKERCOMPOSE_FILE=docker-compose.$DOCKER_COMPOSE.yml
      else
        DOCKERCOMPOSE_FILE=no-cache-docker-compose.$DOCKER_COMPOSE.yml
      fi
    # Resolve the placeholder left by docker-compose-build to THIS runner's cache
    # registry — the one this job also pushes to below. If this runner has none,
    # drop the entry and fall back to the registry.gitlab.com refs.
    - |
      if [ -n "$BUILDKIT_CACHE_REGISTRY" ]; then
        sed -i "s#LOCAL_BUILDKIT_CACHE#$BUILDKIT_CACHE_REGISTRY#g" docker-compose*.yml
      else
        sed -i "/LOCAL_BUILDKIT_CACHE/d" docker-compose*.yml
      fi
    - echo $IMAGE_DEPENDENCY isard-$IMAGE | xargs -n1 docker-compose -f $DOCKERCOMPOSE_FILE build
    - docker tag $CI_REGISTRY_IMAGE/$IMAGE:$CI_COMMIT_REF_SLUG $CI_REGISTRY_IMAGE/$IMAGE:git-$CI_COMMIT_SHORT_SHA
    - docker push $CI_REGISTRY_IMAGE/$IMAGE:git-$CI_COMMIT_SHORT_SHA
    - docker push $CI_REGISTRY_IMAGE/$IMAGE:$CI_COMMIT_REF_SLUG
    - |
      if [ -n "$BUILDKIT_CACHE_REGISTRY" ]; then
        docker tag $CI_REGISTRY_IMAGE/$IMAGE:$CI_COMMIT_REF_SLUG $BUILDKIT_CACHE_REGISTRY/isard/isardvdi/$IMAGE:latest
        docker push $BUILDKIT_CACHE_REGISTRY/isard/isardvdi/$IMAGE:latest
      fi

unit-test-engine:
  # Engine tests under ``engine/services/threads`` and ``engine/models``
  # import libvirt + paramiko + ``changefeed_models`` at module load,
  # so running them in a generic python image is impractical. Re-use
  # the freshly-built engine image as the runner — it already has the
  # full runtime (libvirt-dev, paramiko, ``/opt/isardvdi/asyncapi_changefeed``
  # on PYTHONPATH). Overlay the source tree via ``docker cp`` into
  # ``/isard`` and pytest against it.
  stage: test
  needs: ["docker-image", "gen-protobuf"]
  image: "docker:${DOCKER_VERSION}@sha256:2a232a42256f70d78e3cc5d2b5d6b3276710a0de0596c145f627ecfae90282ac"
  services:
    - name: "docker:${DOCKER_VERSION}-dind-rootless@sha256:7c3e797187e43738220462658f4586572cbd3bf009f728b21e34d9c5c06ce431"
      alias: docker
  before_script:
    - apk add bash git
    - timeout 300 sh -c 'until docker info >/dev/null 2>&1; do sleep 2; done'
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
  script:
    - ENGINE_IMAGE="$CI_REGISTRY_IMAGE/engine:git-$CI_COMMIT_SHORT_SHA"
    - docker pull "$ENGINE_IMAGE"
    - mkdir -p "$CI_PROJECT_DIR/ci-results"
    - |
      # The container writes its junit reports to /ci-results, which is inside the
      # ephemeral dind container (not $CI_PROJECT_DIR), so we docker cp them back
      # into a project-relative dir afterwards for the junit artifact. Keep the
      # container (no --rm) so the cp works even when the tests fail.
      set +e
      docker run --name engine-tests \
        -v "$CI_PROJECT_DIR:/build:ro" \
        -w /isard \
        "$ENGINE_IMAGE" \
        sh -c '
          set -e
          mkdir -p /ci-results
          cp -r /build/engine/engine/. /isard/
          cp -r /build/component/_common/src/isardvdi_common /opt/isardvdi/isardvdi_common 2>/dev/null || true
          rc=0
          pytest engine/models engine/controllers engine/services/threads engine/services/lib initdb -q --tb=short --junitxml=/ci-results/report.xml || rc=1
          # engine/services/db is stubbed as MagicMock by conftest.py; its tests
          # pop the stub and load the real module, which only works under the
          # importlib import mode. Run it as a separate invocation so the default
          # prepend-mode suite above is unaffected.
          pytest --import-mode=importlib engine/services/db -q --tb=short --junitxml=/ci-results/report-db.xml || rc=1
          exit $rc
        '
      RC=$?
      docker cp engine-tests:/ci-results/. "$CI_PROJECT_DIR/ci-results/" || true
      docker rm -f engine-tests >/dev/null 2>&1 || true
      exit $RC
  artifacts:
    when: always
    reports:
      junit:
        - ci-results/report.xml
        - ci-results/report-db.xml

test-e2e:
  stage: test
  cache:
    - *cache_bun
    - *cache_e2e
  when: manual
  tags:
    - gpu
    - kvm
  needs: ["docker-image", "gen-protobuf"]
  timeout: 30m
  allow_failure: true
  variables:
    DOCKER_TLS_CERTDIR: "/certs-root"
  services:
    - name: "docker:${DOCKER_VERSION}-dind@sha256:2a232a42256f70d78e3cc5d2b5d6b3276710a0de0596c145f627ecfae90282ac"
      variables:
        DOCKER_TLS_CERTDIR: "/certs-root"
      alias: docker
      entrypoint:
        - "sh"
        - "-c"
        - "apk add --no-cache fuse-overlayfs && exec dockerd-entrypoint.sh"
  image: "docker:${DOCKER_VERSION}@sha256:2a232a42256f70d78e3cc5d2b5d6b3276710a0de0596c145f627ecfae90282ac"
  before_script:
    # Get dependencies:
    - |
      # git: build.sh
      # make: seeding the instance
      # libstdc++, libgcc: bun runtime
      apk add git make libstdc++ libgcc

    # Configure the instance
    - cp isardvdi.cfg.example isardvdi.e2e.cfg
    - cat testing/config/isardvdi.e2e.cfg.template >> isardvdi.e2e.cfg
    # CI-only vars appended by the runner
    - echo "DOCKER_IMAGE_PREFIX=${CI_REGISTRY_IMAGE}/" >> isardvdi.e2e.cfg
    - echo "DOCKER_IMAGE_TAG=git-$CI_COMMIT_SHORT_SHA" >> isardvdi.e2e.cfg
    - echo "FRONTEND_MODE=all" >> isardvdi.e2e.cfg
    # /dev/hugepages bind loses the hugetlbfs type through nested dind
    # layers; libvirtd's QEMU driver init aborts unless we point it at a
    # placeholder dir (the hypervisor never exercises hugepages in test).
    - mkdir -p /tmp/isard-real-hugepages
    - echo "HUGEPAGES_PATH=/tmp/isard-real-hugepages" >> isardvdi.e2e.cfg
    # Build the docker-compose file
    - CODEGEN=false ./build.sh
    # Start the instance
    - docker-compose -f docker-compose.e2e.yml pull --quiet
    - docker-compose -f docker-compose.e2e.yml up -d
    ## Download bun
    - docker create --name bun-bin oven/bun:1.3.13-alpine@sha256:4de475389889577f346c636f956b42a5c31501b654664e9ae5726f94d7bb5349
    - docker cp bun-bin:/usr/local/bin/bun /usr/local/bin/bun
    - docker rm bun-bin
  script:
    - make test-e2e
  after_script:
    - docker-compose -f docker-compose.e2e.yml logs &> docker-compose.log
    # Playwright writes [[ATTACHMENT|<path>]] relative to junit.xml; GitLab
    # resolves them from CI_PROJECT_DIR, so prepend the dir prefix.
    - |
      if [ -f testing/e2e/test-results/junit.xml ]; then
        sed -i 's#\[\[ATTACHMENT|#\[\[ATTACHMENT|testing/e2e/test-results/#g' testing/e2e/test-results/junit.xml
      fi
  artifacts:
    when: always
    paths:
      - docker-compose.log
      - testing/e2e/test-results/
      - testing/e2e/playwright-report/
    reports:
      junit: testing/e2e/test-results/junit.xml

integration-real:
  # Runs the Python lifecycle suite (testing/integration/) against a
  # full dind-rootless stack. Uses the same docker-compose.e2e.yml the
  # Playwright job builds, so one stack serves both. Manual trigger +
  # allow_failure: we're still bedding in the TCG hypervisor path; any
  # intermittent archive.org outage shouldn't fail the pipeline.
  stage: test
  cache:
    - <<: *cache_uv
      policy: pull
  when: manual
  tags:
    - gpu
  needs: ["docker-image"]
  timeout: 20m
  allow_failure: true
  variables:
    DOCKER_TLS_CERTDIR: "/certs-root"
  services:
    - name: "docker:${DOCKER_VERSION}-dind-rootless@sha256:7c3e797187e43738220462658f4586572cbd3bf009f728b21e34d9c5c06ce431"
      variables:
        DOCKER_TLS_CERTDIR: "/certs-root"
      alias: docker
  image: "docker:${DOCKER_VERSION}@sha256:2a232a42256f70d78e3cc5d2b5d6b3276710a0de0596c145f627ecfae90282ac"
  before_script:
    - apk add bash git docker-compose python3 curl
    - curl -fsSL -o /tmp/uv.tar.gz https://github.com/astral-sh/uv/releases/download/0.11.23/uv-x86_64-unknown-linux-musl.tar.gz
    - echo "6be47081100ff1ce0ac7e85ba2ac12e32f2ffa6f946d78bf7f24ee9ce3a46181  /tmp/uv.tar.gz" | sha256sum -c -
    - tar -xzf /tmp/uv.tar.gz -C /tmp
    - mv /tmp/uv-x86_64-unknown-linux-musl/uv /usr/local/bin/uv
    - timeout 300 sh -c 'until docker info >/dev/null 2>&1; do sleep 2; done'
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
    - cp isardvdi.cfg.example isardvdi.e2e.cfg
    - echo "USAGE=test" >> isardvdi.e2e.cfg
    - echo "DOMAIN=host.docker.internal" >> isardvdi.e2e.cfg
    - echo "DOCKER_IMAGE_PREFIX=${CI_REGISTRY_IMAGE}/" >> isardvdi.e2e.cfg
    - echo "DOCKER_IMAGE_TAG=git-$CI_COMMIT_SHORT_SHA" >> isardvdi.e2e.cfg
    - echo "ENABLE_STATS=false" >> isardvdi.e2e.cfg
    # FRONTEND_MODE=all keeps the Vue 3 SPA reachable for the e2e suite — the
    # cfg.example default is "deprecated" (Vue 3 redirects to Vue 2) for safe
    # production migrations, but every Vue 3 e2e spec navigates to /frontend/*
    # and would silently land on Vue 2 without this override.
    - echo "FRONTEND_MODE=all" >> isardvdi.e2e.cfg
    # Test runs MUST disable the login rate limiter — cfg.example enables it
    # (good for production), but serial/parallel test logins trip it and the
    # whole run cascades into 429 failures. testing/config/isardvdi.e2e.cfg.template
    # does the same for the test-e2e job.
    - echo "AUTHENTICATION_AUTHENTICATION_LIMITS_ENABLED=false" >> isardvdi.e2e.cfg
    # Redirect nvidia-smi / libnvidia-ml / sriov-manage mounts to an
    # always-present file — rootless dind cannot mkdir /usr/bin/nvidia-smi
    # or /usr/lib/nvidia/sriov-manage to satisfy the default bind. GPU
    # discovery exits cleanly when exec fails.
    - echo "NVIDIA_SMI_PATH=/etc/hostname" >> isardvdi.e2e.cfg
    - echo "NVIDIA_ML_LIB_PATH=/etc/hostname" >> isardvdi.e2e.cfg
    - echo "NVIDIA_SRIOV_MANAGE_PATH=/etc/hostname" >> isardvdi.e2e.cfg
    # /dev/hugepages mount: see test-e2e job comment.
    - mkdir -p /tmp/isard-real-hugepages
    - echo "HUGEPAGES_PATH=/tmp/isard-real-hugepages" >> isardvdi.e2e.cfg
    - CODEGEN=false ./build.sh
    - "sed -i 's|source: /opt/isard/|source: /tmp/isard-real/opt/isard/|g; s|source: /opt/isard-local/|source: /tmp/isard-real/opt/isard-local/|g' docker-compose.e2e.yml"
    - docker compose -f docker-compose.e2e.yml pull
    - docker compose -f docker-compose.e2e.yml up -d
    - uv sync --frozen --package isardvdi-testing --group test --no-editable
    - uv run --group test --package isardvdi-testing python3 testing/db/populate_test_db.py
  script:
    - |
      NETWORK_ID="$(docker inspect -f '{{range .NetworkSettings.Networks}}{{.NetworkID}}{{end}}' isard-portal | tr -d '\n')"
      docker run --rm \
        --network="$NETWORK_ID" \
        -v "$(pwd):/src" -w /src/testing \
        -e E2E_ADMIN_USER=admin_e2e_01 \
        -e E2E_ADMIN_PWD='IsardTest1!' \
        -e E2E_SKIP_VM_BOOT=1 \
        ghcr.io/astral-sh/uv:0.11.23-python3.13-trixie-slim bash -c '
          apt-get update -qq && apt-get install -y -qq curl > /dev/null
          bash /src/testing/integration/wait-for-stack.sh 240
          cd /src && uv sync --frozen --package isardvdi-testing --group test --no-editable
          cd /src/testing && uv run --group test --package isardvdi-testing pytest integration/ -v -m real --tb=short --junitxml=/src/testing/integration/report.xml
        '
  after_script:
    - docker compose -f docker-compose.e2e.yml down || true
  artifacts:
    when: always
    reports:
      junit: testing/integration/report.xml

gemnasium-dependency_scanning:
  variables:
    # Scan all the subdirectories
    DS_MAX_DEPTH: -1
    DS_INCLUDE_DEV_DEPENDENCIES: "false"
    DS_REMEDIATE: "false"

docker-tag:
  stage: release
  # Half of the images tagged and half not is a broken release, so this one
  # opts out of the `interruptible: true` default.
  interruptible: false
  image:
    name: gcr.io/go-containerregistry/crane:debug@sha256:1b1fb24d2b1bb27a9daf81a588157e68463876904e8e537a812edba6284fb252
    entrypoint: [""]
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  parallel:
    matrix:
      - IMAGE: *containers
      - IMAGE: &containers_extra
          - codegen
          - check
          - check-client
          - vpn
          - orchestrator
          - portal
          - video
          - monitor-proxy
  script:
    - test ! -e ".VERSION" && exit 0
    - crane auth login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
    - crane tag $CI_REGISTRY_IMAGE/$IMAGE:git-$CI_COMMIT_SHORT_SHA v$(cat .VERSION | tr '.' '-')

release:
  stage: release
  # semantic-release pushes the tag and creates the release; cancelling it
  # mid-flight leaves one without the other, so it opts out of the default.
  interruptible: false
  cache:
    - *cache_bun
  image: oven/bun:1.3.13-alpine@sha256:4de475389889577f346c636f956b42a5c31501b654664e9ae5726f94d7bb5349
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  before_script:
    - apk add --no-cache git sed
    - bun install --frozen-lockfile
  script:
    - test ! -e ".VERSION" && exit 0
    - 'sed -i "s/^\(\s*DOCKER_IMAGE_TAG: \)$CI_DEFAULT_BRANCH$/\1v$(cat .VERSION | tr "." "-")/" docker-compose*.yml'
    - bunx semantic-release --branches $CI_DEFAULT_BRANCH

container_scanning:
  stage: container-scan
  needs:
    - job: docker-image
      artifacts: false
  variables:
    CI_APPLICATION_REPOSITORY: $CI_REGISTRY_IMAGE/$IMAGE
    CI_APPLICATION_TAG: $CI_COMMIT_REF_SLUG
  parallel:
    matrix:
      - IMAGE: *containers
      - IMAGE: *containers_extra
