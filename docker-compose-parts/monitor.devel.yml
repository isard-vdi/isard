services:
  isard-grafana:
    ports:
      - "3000:3000"

  isard-victoriametrics:
    ports:
      # 9090 (legacy Prometheus port) -> VictoriaMetrics' 8428, bound to the infra IP
      - "${INFRASTRUCTURE_HOST_IP:-0.0.0.0}:9090:8428"
