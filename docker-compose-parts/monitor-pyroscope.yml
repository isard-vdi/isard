services:
  isard-pyroscope:
    container_name: isard-pyroscope
    image: grafana/pyroscope:1.21.1@sha256:23f366803b418ca6b80f510f2dbcec22178a836dfe1fe828261d93273c0a4f97
    user: root
    volumes:
      - /opt/isard/stats/pyroscope:/data
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        tag: "{{.ImageName}}|{{.Name}}|{{.ImageFullID}}|{{.FullID}}"
    networks:
      isard-network:
        ipv4_address: ${DOCKER_NET:-172.31.255}.71
    restart: unless-stopped
    command: >-
      --pyroscopedb.data-path=/data
