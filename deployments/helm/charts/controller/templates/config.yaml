apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "controller.fullname" . }}
  labels:
    {{- include "controller.labels" . | nindent 4 }}
data:
  xds-bootstrap.json: |
    {
      "xds_servers": [{
        "server_uri": "{{ .Values.global.xdsAddr }}",
      }],
      "node": {
        "id": "{{ uuidv4 }}"
      }
    }
