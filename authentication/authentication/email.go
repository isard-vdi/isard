package authentication

import (
	"context"
	"errors"
)

// RequestEmailValidation requests a new email validation. This generates a temporary token
// that gets sent to the provided email
func (a *Authentication) RequestEmailValidation(ctx context.Context, tkn, email string) error {
	return errors.New("not implemented yet")
}

// ValidateEmail validates a email for a user using a token generated by the RequestEmailValidation
func (a *Authentication) ValidateEmail(ctx context.Context, tkn string) error {
	return errors.New("not implemented yet")
}
