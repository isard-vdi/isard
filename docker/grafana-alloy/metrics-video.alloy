prometheus.scrape "metrics_haproxy_video" {
	targets = [{
		__address__ = "isard-video:9090",
		domain      = env("DOMAIN"),
	}]

	forward_to      = [prometheus.remote_write.monitor.receiver]
	job_name        = "haproxy_video"
	scrape_interval = "30s"
}
